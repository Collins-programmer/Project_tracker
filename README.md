# CLI Multi-User Project Management Framework Engine

An elegant object-oriented CLI system application designed to handle task management and work structures across distributed project environments locally.

## 🚀 Setup & System Deployment

1. **Clone the project workspace directory layout** locally to your sandbox run environment.
2. **Install all runtime system module requirements** via pip:
      `pipenv install`